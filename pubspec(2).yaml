
name: dv_photo_app
description: DV-ready photo cropper — Part 3 live overlay + print + Ethiopia queue stubs
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.1
  image_picker: ^1.1.2
  image: ^4.1.7
  path_provider: ^2.1.4
  google_mlkit_face_detection: ^0.11.0
  camera: ^0.11.0+2
  pdf: ^3.10.7
  printing: ^5.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
